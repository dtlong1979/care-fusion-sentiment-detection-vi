# Core DL stack (PhoBERT fine-tuning)
# NOTE: on a CUDA machine/Colab, install the matching torch wheel first (see README),
# then `pip install -r requirements.txt` will skip reinstalling torch.
torch>=2.2,<2.5
transformers==4.44.2
tokenizers>=0.19,<0.20

# Vietnamese word segmentation for PhoBERT (requires Java >= 1.8)
py_vncorenlp==0.1.4

# Affective markers
emoji==2.12.1

# Data / numerics / stats
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
scipy==1.13.1
pyyaml==6.0.2
tqdm==4.66.5

# Dev
pytest==8.3.2
