# IMPORTANT: torch / torchvision are intentionally NOT listed here.
# Colab and Kaggle ship a CUDA-enabled torch; pinning it would downgrade their
# GPU build (and drag numpy/scipy/sklearn down with it). Install torch yourself
# for local / other machines first (see README), then `pip install -r requirements.txt`
# only adds the lightweight, platform-agnostic deps below.
#
# Lower bounds only (no upper caps / exact pins) so we never force-downgrade a
# host's existing scientific stack. The exact resolved versions are recorded at
# run time for reproducibility (Part H).

transformers>=4.40
py_vncorenlp==0.1.4
emoji>=2.10
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
pyyaml>=6.0
tqdm>=4.66
pytest>=8.0
